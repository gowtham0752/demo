https://dhayaaperumal.github.io/demo/index.html

try {

    val listdata = ArrayList<String>()
        for (i in 0 until jArray.length()) {
            listdata.add(jArray.get(i).toString())
        }
    val distinct = listdata.toSet().toList();
     val tempArray = JSONArray()
    val uniqueset: MutableSet<String> = HashSet()
    for (i in 0 until jArray.length()) {
        val jobject = jArray[i] as JSONObject
        val keys: MutableIterator<String> = jobject.keys()
        val it = keys.iterator()
        while (it.hasNext()) {
            val currentKey = it.next()
            val jsonvalue = jobject[currentKey] as String
            if (uniqueset.contains(jsonvalue)) {
                continue
            } else {
                uniqueset.add(jsonvalue)
                tempArray.put(jArray[i])
            }
        }
    }
    jArray1 = tempArray
    
}finally{
     js = "var cachedContacts = ${jArray1};"
 }
